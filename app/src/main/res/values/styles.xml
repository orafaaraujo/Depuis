<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/main_primary</item>
        <item name="colorPrimaryDark">@color/main_primary_dark</item>
        <item name="colorAccent">@color/main_accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="NewFactTheme" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/new_fact_primary</item>
        <item name="colorPrimaryDark">@color/new_fact_primary_dark</item>
        <item name="colorAccent">@color/new_fact_accent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="new_fact_date_time">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:drawablePadding">12dp</item>
        <item name="android:clickable">true</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@color/new_fact_accent</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
