<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.orafaaraujo.depuis.viewModel.MainViewModel" />
    </data>

    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.orafaaraujo.depuis.view.activity.MainActivity">

        <android.support.design.widget.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/AppTheme.AppBarOverlay">

            <android.support.design.widget.CollapsingToolbarLayout
                android:id="@+id/main_collapsing_toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:collapsedTitleGravity="center"
                app:expandedTitleGravity="center"
                app:layout_scrollFlags="scroll|exitUntilCollapsed|snap"
                tools:text_font="@{`pacifico-regular`}">

                <View
                    android:id="@+id/expandedImage"
                    android:layout_width="match_parent"
                    android:layout_height="196dp"
                    android:background="@drawable/toolbar_background"
                    app:layout_collapseMode="parallax" />

                <android.support.v7.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="64dp" />

            </android.support.design.widget.CollapsingToolbarLayout>

        </android.support.design.widget.AppBarLayout>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/main_recycler_fact"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="@{viewModel.mListVisibility ? View.VISIBLE : View.INVISIBLE}"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            tools:adapter="@{viewModel.adapter}"
            tools:divider_item="@{viewModel.dividerItemDecoration}"
            tools:item_touch="@{viewModel.ItemTouch}"
            tools:layout_manager="@{viewModel.layoutManager}"
            tools:on_scroll_listener="@{viewModel.scrollListener}" />

        <include
            android:id="@+id/main_empty_list_layout"
            layout="@layout/activity_main_empty_list"
            android:visibility="@{viewModel.mEmptyVisibility ? View.VISIBLE : View.INVISIBLE}"
            tools:click="@{viewModel.fabClick}" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/main_fab_new_fact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_margin"
            android:onClick="@{viewModel.fabClick}"
            app:srcCompat="@drawable/ic_plus_one_24dp"
            tools:fab_animation="@{viewModel.mShow}" />

    </android.support.design.widget.CoordinatorLayout>
</layout>